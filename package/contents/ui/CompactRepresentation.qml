import QtQuick
import org.kde.kirigami as Kirigami

Item {
    id: root

    Kirigami.Icon {
        anchors.centerIn: parent
        width: Kirigami.Units.iconSizes.medium
        height: width
        source: "catherder"
    }
}
